# Changesets

Run `yarn changeset` to record package changes that should be released.

When a changeset lands on `master`, the release workflow will open or update a version PR. After that PR is merged, the workflow publishes any unpublished package versions to npm.
