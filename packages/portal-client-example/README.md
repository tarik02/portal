# portal client example

## run

```bash
yarn workspace @tarik02/portal-client-example dev
```

## notes

- open the app in the browser after `dev` starts
- rebuild with `yarn workspace @tarik02/portal-client-example build` when you want to refresh the embedded dist output
